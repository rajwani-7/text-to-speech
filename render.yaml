services:
  - type: web
    name: text-to-speech
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      gunicorn app:app --chdir api --bind 0.0.0.0:$PORT
    pythonVersion: 3.11.9
